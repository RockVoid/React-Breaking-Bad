import React from 'react';
import Spinner from '../../img/spinner.gif';

const Load = () => {
    return (
        <img
            className="spinner"
            src={Spinner}
            alt=""
        />
    );
}

export default Load;